# jakerayner96.github.io

The live site for Jake Rayner: UX, UI and Product Design portfolio, plus the
Studio freelance offer.

Plain HTML, CSS and JavaScript with no build step. GSAP, ScrollTrigger and
Lenis load from a CDN. Served by GitHub Pages from `main`.

This repo is generated output. It is published from a private source repo, so
edits made here get overwritten on the next deploy.
